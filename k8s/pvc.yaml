apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hyperliquid-bot-pvc
  namespace: trading
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  volumeName: hyperliquid-bot-pv
  storageClassName: ""
